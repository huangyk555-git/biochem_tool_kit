# SVG 结构式资源

氨基酸条目的 `structureAsset` 已预留为空。将独立 SVG 放在此目录，修改 JSON 中该条目的 `structureAsset` 为文件名，然后重新构建。

资源建议：`viewBox="0 0 400 180"`，透明或浅色背景；使用静态路径、文字和化学键，不含脚本、远程图片和外部字体。请人工核对价态、取代位置、质子化状态和立体化学。

`aldehyde.svg`、`ketone.svg`、`amide.svg` 是自行绘制的连接关系示例。完整结构式未提供时，应用展示 JSON 中的缩合通式。
